# Core Framework
Django==5.1.1

# Environment Variables
python-dotenv==1.0.1

# Image Processing (for ImageField in models)
Pillow==12.1.1

# Django Dependencies (automatically installed with Django, but listed for completeness)
asgiref==3.8.1
sqlparse==0.5.1
tzdata==2024.2

# Optional: For better environment variable management (alternative to python-dotenv)
# django-environ==0.11.2
